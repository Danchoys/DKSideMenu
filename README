DKSideMenu is an easy-to-use component which allows to create iPad apps with a nice side menu, supporting both landscape and portrait content view controllers. It takes care of transitions between controllers, managing UINavigationBar instances. It may be used with storyboards using two types of custom segues, included in this library.
